[package]
name = "aggregate-mldsa-merkle"
version = "0.1.0"
edition = "2021"

[dependencies]
sp1-zkvm = { version = "6", features = ["verify"] }
sha2 = { version = "0.10", default-features = false }

[[bin]]
name = "aggregate-mldsa-merkle"
path = "src/main.rs"

[profile.release]
lto = true

# Detacha este crate do workspace do host, mesmo motivo dos outros guests.
[workspace]
